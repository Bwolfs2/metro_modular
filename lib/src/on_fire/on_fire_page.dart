import 'package:flutter/material.dart';

class OnFirePage extends StatefulWidget {
  @override
  _OnFirePageState createState() => _OnFirePageState();
}

class _OnFirePageState extends State<OnFirePage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("OnFire"),
        backgroundColor:  Colors.teal[200],
      ),
      body: Column(
        children: <Widget>[],
      ),
    );
  }
}
